This folder contains some experiments and works in progress such as:

- An initial attempt at implementation using Julia R bindings.
- An unfinished attempt to fetch tiles from the OSM servers automatically.

